-- ══════════════════════════════════════════════════════════════════════
-- حساباتُ القادة — قالبٌ يُملأ ويُشغَّل مرّةً في محرّر SQL بلوحة Supabase
-- ══════════════════════════════════════════════════════════════════════
--
-- ⛔ **لا تُكتب فيه أسماءٌ ولا بُرُدٌ حقيقية ثم يُلتزَم في المستودع.**
-- قاعدةُ المستودع: لا أسماءَ أشخاصٍ في موادّ النادي (واستثناؤها الوحيد
-- `content/leadership.ts`). فيُنسخ هذا القالب، ويُملأ **خارج المستودع**،
-- ويُلصق في محرّر Supabase.
--
-- ── ما الذي يفعله ─────────────────────────────────────────────────────
-- يُنشئ صفَّ صلاحيةٍ لكلّ رئيس لجنةٍ أو مشروعٍ ونائبِه. ولا يُنشئ حسابَ
-- دخول: الحسابُ يُصنع تلقائيًّا أوّلَ ما يطلب صاحبُه رابطَ الدخول من
-- `/admin/login` — والرابطُ لا يُرسَل أصلًا لبريدٍ ليس في هذا الجدول.
--
-- ── النطاق ────────────────────────────────────────────────────────────
-- ⚠️ **باللجنة لا بالوحدة.** `committee:public-relations` تغطّي وحداتها
-- الثلاث كلَّها بمطابقة البادئة — وكتابةُ الوحدات فرادى تقسم ما لا ينقسم
-- وتُربك عدّادَ المقابلات.
--
-- ⚠️ **والبريدُ صغيرًا كلُّه.** محفِّزُ `staff_normalize_email_trg` يُصغّره
-- عند الكتابة، لكنّ كتابتَه صغيرًا من البداية تجنّبك حيرةَ من يقارن.
--
-- ── الأدوار ───────────────────────────────────────────────────────────
--   leader  يرى من ذكر نطاقَه في رغباته الثلاث، ويعمل على من هو عند رتبته
--   admin   يرى كلَّ شيء، ويفتح المراحل. لا يُعطى إلّا للرئاسة.
--
-- ══════════════════════════════════════════════════════════════════════

insert into public.staff (email, role, scopes, display_name, label) values

  -- ── اللجان الأربع ───────────────────────────────────────────────────
  -- لجنة العلاقات العامة والشراكات — ٣ وحدات
  ('__رئيس@مثال',  'leader', '{committee:public-relations}',   '__الاسم', 'رئيس لجنة العلاقات العامة'),
  ('__نائب@مثال',  'leader', '{committee:public-relations}',   '__الاسم', 'نائب رئيس لجنة العلاقات العامة'),

  -- لجنة الموارد المالية والتنظيمية — ٣ وحدات
  ('__رئيس@مثال',  'leader', '{committee:finance-operations}', '__الاسم', 'رئيس لجنة الموارد المالية'),
  ('__نائب@مثال',  'leader', '{committee:finance-operations}', '__الاسم', 'نائب رئيس لجنة الموارد المالية'),

  -- لجنة الموارد البشرية — بلا وحدات
  ('__رئيس@مثال',  'leader', '{committee:human-resources}',    '__الاسم', 'رئيس لجنة الموارد البشرية'),
  ('__نائب@مثال',  'leader', '{committee:human-resources}',    '__الاسم', 'نائب رئيس لجنة الموارد البشرية'),

  -- اللجنة الإعلامية — ٥ وحدات
  ('__رئيس@مثال',  'leader', '{committee:media}',              '__الاسم', 'رئيس اللجنة الإعلامية'),
  ('__نائب@مثال',  'leader', '{committee:media}',              '__الاسم', 'نائب رئيس اللجنة الإعلامية'),

  -- ── المشاريع الخمسة المفتوحة ────────────────────────────────────────
  ('__رئيس@مثال',  'leader', '{project:misology}',             '__الاسم', 'قائد MISology'),
  ('__نائب@مثال',  'leader', '{project:misology}',             '__الاسم', 'نائب قائد MISology'),

  ('__رئيس@مثال',  'leader', '{project:misthon}',              '__الاسم', 'قائد MISthon'),
  ('__نائب@مثال',  'leader', '{project:misthon}',              '__الاسم', 'نائب قائد MISthon'),

  ('__رئيس@مثال',  'leader', '{project:job-shadowing}',        '__الاسم', 'قائد Job Shadowing'),
  ('__نائب@مثال',  'leader', '{project:job-shadowing}',        '__الاسم', 'نائب قائد Job Shadowing'),

  ('__رئيس@مثال',  'leader', '{project:impact}',               '__الاسم', 'قائد Impact'),
  ('__نائب@مثال',  'leader', '{project:impact}',               '__الاسم', 'نائب قائد Impact'),

  ('__رئيس@مثال',  'leader', '{project:intermission}',         '__الاسم', 'قائد InterMission'),
  ('__نائب@مثال',  'leader', '{project:intermission}',         '__الاسم', 'نائب قائد InterMission')

-- ⚠️ **يُعاد تشغيلُه بلا ضرر.** من كان موجودًا يُحدَّث نطاقُه واسمُه ولا
-- يُنشأ ثانيةً — فلو أخطأتَ في نطاقٍ صحّحه هنا وأعد التشغيل.
on conflict (email) do update set
  role         = excluded.role,
  scopes       = excluded.scopes,
  display_name = excluded.display_name,
  label        = excluded.label;


-- ══════════════════════════════════════════════════════════════════════
-- فحصٌ بعد التشغيل — يجب أن يطابق العددُ ما تتوقّعه
-- ══════════════════════════════════════════════════════════════════════
--
--   select role, count(*) from public.staff group by role;
--
-- وأنّ كلَّ طلبٍ له قائدٌ يراه (يجب أن يرجع صفرَ صفوف):
--
--   select a.choice1, count(*)
--     from public.applications a
--    where not exists (
--            select 1 from public.staff s
--             where s.role = 'leader'
--               and private.choice_in_scopes(a.choice1, s.scopes))
--    group by a.choice1;
