/**
 * بنية التنقّل — مصدر واحد يقرأ منه الشريط العلوي والتذييل.
 *
 * ⚠️ **الشريط أُعيد ترتيبه في ٣ أغسطس ٢٠٢٦ بطلب مباشر:** ستة عناصر لا غير.
 * وهذا يلغي ترتيبين كانا موثَّقين هنا كتوجيه من الرئاسة:
 *   · «المشاريع الستة ترتفع إلى الشريط العلوي» — صارت رابطًا واحدًا
 *     إلى صفحة المشاريع بدل رفٍّ يعرض المشاريع بأسمائها وألوانها.
 *   · «اللجان» و«المقالات» خرجتا من الشريط إلى التذييل.
 * يُراجَع هذا مع الرئاسة قبل النشر.
 *
 * «حول النادي» صار «من نحن»، ويجمع النبذة والهيكلة واللجان والشراكات.
 */

export type NavLink = {
  label: string;
  href: string;
};

export type NavSection = {
  label: string;
  href: string;
  children?: readonly NavLink[];
};

/**
 * «من نحن» — مظلّة ما يشرح النادي نفسه.
 * «اللجان» تحتها: خرجت من الشريط العلوي، ولا تُترك بلا طريق إليها.
 */
export const ABOUT_SECTION: NavSection = {
  label: "من نحن",
  href: "/about",
  children: [
    { label: "نبذة عن النادي", href: "/about" },
    /* ⚠️ **عادت مُعلَنةً باسمها الجديد (١٢ أغسطس ٢٠٢٦).** كانت «الهيكل
       القيادي» ومرفوعةً بلا رابط؛ وصارت «الهيكل الإداري» وصفحتُها تقود
       إلى إعلانٍ في إكس — فلها ما تقوله، فتُعلَن. */
    { label: "الهيكل الإداري", href: "/about/structure" },
    { label: "اللجان", href: "/committees" },
    /* ⚠️ **«الشراكات» نُزعت — الصفحة مرفوعةٌ مؤقّتًا (١٢ أغسطس ٢٠٢٦).**
       الصفحة تبقى تُخدَم بوجهة «قريبًا» لمن وصل من رابطٍ قديم، لكنها لا
       تُعلَن. تعود مع الصفحة — انظر قائمة الاسترجاع في رأس
       `app/about/partnerships/page.tsx`. */
  ],
};

/** الشريط العلوي — ستة عناصر، بالترتيب المطلوب */
export const NAVIGATION: readonly NavSection[] = [
  { label: "الرئيسية", href: "/" },
  { label: "الإنجازات", href: "/achievements" },
  /* ⚠️ **الرابط باقٍ والصفحةُ مغلقة — بقرار الإدارة صراحةً.** وجهتُه اليوم
     صفحةُ «ترقّبونا» حتى تجهز شعارات المشاريع. والوجهة صادقةٌ لا مخيّبة:
     تقول ما يحدث ولماذا، وتُخرج الزائرَ إلى التقديم أو السؤال. وحذفُ
     الرابط كان يُخفي أن للنادي مشاريع أصلًا — وهي أبرز ما فيه. */
  { label: "المشاريع", href: "/projects" },
  ABOUT_SECTION,
  { label: "تواصل معنا", href: "/contact" },
  { label: "الأسئلة الشائعة", href: "/faq" },
];

/**
 * عمود «أقسام» في التذييل.
 * فيه ما خرج من الشريط — فلا تبقى صفحة بلا طريق إليها من أي مكان.
 */
export const FOOTER_LINKS: readonly NavLink[] = [
  { label: "الإنجازات", href: "/achievements" },
  /* الصفحة مغلقةٌ مؤقّتًا والرابط باقٍ — انظر `NAVIGATION` أعلاه */
  { label: "المشاريع", href: "/projects" },
  /* ⚠️ **رُفع «المقالات» — بقرارٍ في ١٤ أغسطس ٢٠٢٦:** «شِل قسم المقالات من
     الفوتر، ما فيه مقالات حاليًا، بفضّي لها بعدين». و`posts.json` فارغٌ
     فعلًا (`[]`)، فالصفحة كانت تعرض عنوانًا ووصفًا ولا شيء تحتهما.

     ⚠️ **ولمّا رُفع الرابط لم يبقَ للصفحة طريقٌ** — وقاعدة هذا الملفّ فوقه:
     «فلا تبقى صفحة بلا طريق إليها من أي مكان». فعوملت معاملةَ المشاريع:
     `noindex` في `posts/page.tsx` وخروجٌ من `sitemap.ts` — فلا تُفهرَس
     صفحةٌ فارغةٌ لا يصل إليها زائر.

     وإعادتُها ثلاث خطوات: يُملأ `posts.json`، ويُعاد هذا السطر، ويُنزع
     `noindex` وتُعاد سطور الخريطة. */
  { label: "الأسئلة الشائعة", href: "/faq" },
  { label: "قدِّم للعضوية", href: "/join" },
];

/** رابط الإجراء الوحيد في الشريط — منفصل عن التنقّل عمدًا. */
export const PRIMARY_ACTION = {
  label: "قدِّم للعضوية",
  href: "/join",
} as const;
