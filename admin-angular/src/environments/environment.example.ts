/**
 * قالبُ الإعداد — **يُنسخ إلى `environment.ts` ولا يُحرَّر مكانَه.**
 *
 *     npm run setup:env      (يولّده من `.env.local` في مشروع Next)
 *
 * ⚠️ **المفتاحان هنا عامّان بطبيعتهما، ومع ذلك لا يدخلان git.** المفتاح
 * `sb_publishable_…` مصمَّمٌ ليعمل في المتصفّح: الحاجزُ الأمنيّ هو `RLS`
 * في القاعدة لا سرّيةُ المفتاح، وهو يُشحن اليوم في كل صفحةٍ من الموقع.
 * فإخفاؤه ليس أمانًا — لكنّ إبقاءه خارج المستودع يحفظ قاعدةً واحدةً لا
 * استثناء فيها: **لا مفتاحَ في git**. والقاعدةُ التي لها استثناءٌ واحد
 * تُخرَق يومًا بمفتاحٍ يظنّه أحدُهم عامًّا وليس كذلك.
 *
 * ⛔ **و`SUPABASE_SERVICE_ROLE_KEY` لا يُكتب هنا ولا في أي ملفٍّ من هذا
 * التطبيق.** ذاك يتخطّى `RLS` كلَّه، وتطبيقُ متصفّحٍ يحمله يعني أن أيّ
 * زائرٍ يفتح أدوات المطوّر يقرأ كلَّ طلبٍ في القاعدة برقم أحواله.
 */
export const environment = {
  production: false,
  supabaseUrl: "https://<project-ref>.supabase.co",
  supabasePublishableKey: "sb_publishable_<...>",
};
